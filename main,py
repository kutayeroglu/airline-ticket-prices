def load_data():
    """
    Loads the dataset from a source (CSV, SQL, API, etc.).
    Returns raw data (e.g., as a Pandas DataFrame).
    """
    # TODO: Implement data loading logic
    pass

def process_data(raw_data):
    """
    Cleans and transforms the data. 
    Splits into train, val, test sets. 
    Returns processed datasets.
    """
    # TODO: Data cleaning, feature engineering, splitting
    pass

def train_model(train_set, val_set):
    """
    Trains a model on the training set, optionally validates on val_set.
    Returns a trained model.
    """
    # TODO: Model training logic
    pass

def evaluate_model(model, test_set):
    """
    Evaluates the trained model on the test set.
    Computes and prints/saves relevant metrics.
    """
    # TODO: Compute accuracy, RMSE, MAE, etc.
    pass

def main():
    raw_data = load_data()

    train_set, val_set, test_set = process_data(raw_data)
    
    model = train_model(train_set, val_set)
    
    evaluate_model(model, test_set)
    

if __name__ == "__main__":
    main()
